const config = {
    user: "root",
    password: "root",
    port: 8889, // for windows 3306
    host: "localhost",
    // database: "marvelcharacters",
    database: "favthings"
}

module.exports = config;  // too make it public