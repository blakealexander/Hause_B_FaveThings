{{!-- <h1>{{title}}</h1>
<p>Welcome to {{title}}</p> --}}

<section id="favs">
    {{!-- for apple in fruits --}}
    {{!-- this is a loop with handle bars and a close --}}
    {{#each images}} 
    <div class="hero-container">
     <a href="/{{name}}">
      <img src="/images/{{image}}" alt="images">
     </a>
     <h3 class="hero-name">{{name}}</h3>
    </div>

    
    {{/each}}
</section>
